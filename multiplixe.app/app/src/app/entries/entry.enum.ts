export enum EntryStatusEnum {
    none = 0,
    invalid = 1,
    valid = 2
}

export enum EntryCodeEnum {
    none = 0,
    already = 1,
    empty = 2,
    invalid = 3,
    inactive = 4,
    erro = 5,
    notfound = 6,
    different = 7,
    maxlength = 8
}